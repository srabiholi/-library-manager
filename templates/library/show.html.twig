{% extends "base.html.twig" %}

{% block title %}{% endblock %}
{% block stylesheets %}
<link href="{{ asset('asset/css/showLibrary.css') }}" rel="stylesheet">
{% endblock %}

{% block body %}
    <div class="d-flex flex-column align-items-center mt-5">
        <div class="item p-2"><a class="d-flex align-items-center justify-content-center" href="{{ path('book_list', {'library': library.id})}}">{{ library.name }}</a></div>
        <div class="item p-2 d-flex flex-column align-items-center"><a class="d-flex align-items-center justify-content-center" href="{{ path('book_create', {'library': library.id})}}">Ajouter un livre</a></div>
        <div class="item p-2"><a class="d-flex align-items-center justify-content-center" href="#">Ajouter un journal</a></div>
        <div class="item p-2"><a class="d-flex align-items-center justify-content-center" href="#">Ajouter un magazine</a></div>
        <div class="item p-2 "><a class="d-flex align-items-center justify-content-center" href="#">Statistique</a></div>
    </div>

{% endblock %}